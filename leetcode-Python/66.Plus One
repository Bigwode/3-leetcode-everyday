class Solution:
    def plusOne(self, digits):
        """
        :type digits: List[int]
        :rtype: List[int]
        """
        num = int(''.join(str(x) for x in digits))
        res = num + 1
        return [int(x) for x in list(str(res))]
        
        
