class Solution:
    def removeElement(self, nums, val):
        """
        :type nums: List[int]
        :type val: int
        :rtype: int
        """
        # Method 1
        while val in nums:
            nums.remove(val)
        return len(nums)
        
        # Method 2
#         id = 0
#         while id < len(nums):
#             if nums[id]  == val:
#                 nums[id] = nums[-1]
#                 del nums[-1]
#             else:
#                 id += 1
#         return len(nums)
                
