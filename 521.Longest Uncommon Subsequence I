class Solution:
    def findLUSlength(self, a, b):
        """
        :type a: str
        :type b: str
        :rtype: int
        """
        if len(a) <= len(b):
            return len(b) if a!= b else -1
        else:
            return len(a)
