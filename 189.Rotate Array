class Solution:
    def rotate(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: void Do not return anything, modify nums in-place instead.
        """
        # Method 1
        # for i in range(k):
        #     x = nums.pop()
        #     nums.insert(0,x)
        # Method 2
        k = k%len(nums)
        nums[:] = nums[-k:] + nums[:-k]
